{
  "name": "@rippledb/test-e2e-http",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@rippledb/client": "workspace:*",
    "@rippledb/core": "workspace:*",
    "@rippledb/db-memory": "workspace:*",
    "@rippledb/remote-http": "workspace:*",
    "@rippledb/store-memory": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run --pool=threads",
    "test:watch": "vitest --pool=threads"
  }
}
