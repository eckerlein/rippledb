{
  "name": "@converge/test-e2e-basic",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@converge/client": "workspace:*",
    "@converge/core": "workspace:*",
    "@converge/db-memory": "workspace:*",
    "@converge/store-memory": "workspace:*",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run --pool=threads",
    "test:watch": "vitest --pool=threads"
  }
}

